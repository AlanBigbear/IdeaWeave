# 服务器部署：MySQL（首启自动导入 dump）+ FastAPI（托管前端 dist + API）
#
# 用法：
#   1. 生成数据 dump：  cd backend && .venv/bin/python3 scripts/dump_mysql.py
#      （产物在 deploy/ideaweave_full.sql）
#   2. 设置密钥（二选一）：
#        a. 在当前目录建 .env 文件，写入 MYSQL_ROOT_PASSWORD / MYSQL_PASSWORD / JWT_SECRET / DEFAULT_LLM_API_KEY
#        b. 或先 export 这些环境变量
#   3. 启动：  docker compose -f docker-compose.prod.yml up -d --build
#   4. 访问：  http://服务器IP:8000
#
# 注意：dump 只在 MySQL 数据卷「首次为空」时自动导入。
#      想强制重灌：  docker compose -f docker-compose.prod.yml down -v

services:
  db:
    image: mysql:8.0
    container_name: ideaweave-db
    restart: unless-stopped
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-please-change-root}
      MYSQL_DATABASE: ideaweave
      MYSQL_USER: ideaweave
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-please-change-user}
    volumes:
      - db_data:/var/lib/mysql
      # 首次初始化依次导入完整数据与公共试用空间；已有 volume 不会重复执行
      - ./deploy/ideaweave_full.sql:/docker-entrypoint-initdb.d/01_ideaweave.sql:ro
      - ./deploy/ideaweave_trial.sql:/docker-entrypoint-initdb.d/02_ideaweave_trial.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p\"$$MYSQL_ROOT_PASSWORD\" --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s

  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: ideaweave-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql+pymysql://ideaweave:${MYSQL_PASSWORD:-please-change-user}@db:3306/ideaweave?charset=utf8mb4
      MYSQL_FALLBACK_SQLITE: "false"
      JWT_SECRET: ${JWT_SECRET:-please-change-this-jwt-secret-32bytes+}
      DATA_DIR: /app/data
      DEFAULT_LLM_BASE_URL: ${DEFAULT_LLM_BASE_URL:-https://api.deepseek.com/v1}
      DEFAULT_LLM_MODEL: ${DEFAULT_LLM_MODEL:-deepseek-v4-pro}
      DEFAULT_LLM_FAST_MODEL: ${DEFAULT_LLM_FAST_MODEL:-deepseek-v4-flash}
      DEFAULT_LLM_API_KEY: ${DEFAULT_LLM_API_KEY:-}
      TRIAL_ENABLED: ${TRIAL_ENABLED:-true}
      TRIAL_USERNAME: ${TRIAL_USERNAME:-demo}
      TRIAL_ANIME_USERNAME: ${TRIAL_ANIME_USERNAME:-demo-anime}
      TRIAL_PET_USERNAME: ${TRIAL_PET_USERNAME:-demo-pet}
      TRIAL_RESET_MINUTES: ${TRIAL_RESET_MINUTES:-60}
      TRIAL_JWT_EXPIRE_MINUTES: ${TRIAL_JWT_EXPIRE_MINUTES:-120}
      TRIAL_LOGIN_REQUESTS_PER_MINUTE: ${TRIAL_LOGIN_REQUESTS_PER_MINUTE:-20}
      TRIAL_GENERATION_REQUESTS_PER_WINDOW: ${TRIAL_GENERATION_REQUESTS_PER_WINDOW:-5}
      TRIAL_GENERATION_WINDOW_SECONDS: ${TRIAL_GENERATION_WINDOW_SECONDS:-600}
      TRIAL_GENERATION_MAX_CONCURRENCY: ${TRIAL_GENERATION_MAX_CONCURRENCY:-2}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    volumes:
      - backend_data:/app/data

volumes:
  db_data:
  backend_data:
